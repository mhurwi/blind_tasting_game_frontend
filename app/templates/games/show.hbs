<div class="row">
  <div class="col-sm-12">
    <h4>Game Master View</h4>
    <p>This is the page for a game master to setup and monitor a game.</p>
    <p>Players should not be allowed to see this game.  They should got to the /games/:game_id/play page.</p>
  </div>
</div>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3>Tasting: {{ name }}</h3>
  </div>
  <div class="panel-body">
    <p>Stuff to be tasted:</p>
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Position</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        {{#each material in materials}}
        <tr>
          <td>{{material.name}}</td>
          <td>{{material.position}}</td>
          <td><i class="fontastic-trash" {{ action 'removeMaterial' material.id }}></i></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <hr>
    <form>
      {{input
      type="text"
      id="new-material"
      placeholder="Name of material..."
      value=materialName
      }}
      <button type="submit" class="btn btn-default" {{action "addMaterial" }}>Add Material</button>
    </form>
    <div>
      {{#if prepared }}
        <p><span class="label label-success">Ready to play</span></p>
      {{else}}
        <p>Add some materials.</p>
        <p>When you've added them all, click 'All Set!'</p>
        <p>
          <button class="btn btn-large btn-success" {{action "setPrepared"}}>All set!</button>
        </p>
      {{/if}}
    </div>
  </div>
</div>
<div class="row">
  <div class="col-sm-12">
    <p>
    <button class="btn btn-small btn-danger" {{ action "deleteGame" }}>Delete Game</button>
    </p>
  </div>
</div>